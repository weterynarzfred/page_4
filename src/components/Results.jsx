import React from 'react';
import { connect } from 'react-redux';

function Results(props) {
  return <div className="Results">
    <p>// TODO: Results will go here.</p>
  </div>;
}

export default connect(state => {
  return {};
})(Results);
