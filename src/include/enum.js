const optionTypes = {
  INTEGER: 'integer',
  TEXT: 'text',
  SELECT: 'select',
  INSTANCER: 'instancer',
  GROUP: 'group',
};

const actions = {
  SELECT_OPTION: 'SELECT_OPTION',
  CHANGE_PATH: 'CHANGE_PATH',
};

export { optionTypes, actions };
